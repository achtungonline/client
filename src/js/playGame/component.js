var React = require('react');

module.exports = React.createClass({
    displayName: 'PlayGame',
    render: function () {
        return (
            <div>
                Play game component
            </div>
        );
    }
});
